<resources>
    <!-- Dark counterpart, so the window doesn't flash white before Compose draws. -->
    <style name="Theme.EmployeeProfileManager" parent="android:Theme.Material.NoActionBar" />
</resources>
