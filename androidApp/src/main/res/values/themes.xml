<resources>
    <!-- Compose draws everything; the platform theme only sets the window behind it. -->
    <style name="Theme.EmployeeProfileManager" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
